"use strict";
// React is capitalized for alignment React.createElement pragma default in
// runtime JSX transformation. Do not lowercase at this time.
const React = require("react");
module.exports = React.createClass({
  render() {
    return (<div>test</div>);
  }
});
